eureka:
  client:
    service-url:
      defaultZone: http://${SERVER_HOST}:${EUREKA_PORT}/eureka/
      registry-fetch-interval-seconds: 5
      initial-instance-info-replication-interval-seconds: 5

  instance:
    prefer-ip-address: true